# Brand assets

This directory contains copies of the approved Daily Digital logo and favicon assets referenced by `site.config.json`.
Keep copies byte-identical to the canonical files in the main Daily Digital repository's `brand/` directory.
