{
  "name": "daily-digital-coming-soon",
  "private": true,
  "version": "0.1.0",
  "description": "Daily Digital's static localized coming-soon launch site.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node server.mjs",
    "build": "node scripts/build.mjs",
    "check": "npm run build && node scripts/check-build.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
